name: setup-go-workspace
version: 1.3.2
description: Adds the project to the Go workspace directory hierarchy.
properties:
  gopath:
    type: string
    default: "$GOPATH"
    required: false
  package-dir:
    type: string
    required: false
